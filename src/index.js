
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);


// const names=['alice','bob','sherry'];
// const res = names.map((name)=>{
//     return <p>+name[0].toUpperCase()+name.slice(1)+</p>
// })

// console.log(res);
